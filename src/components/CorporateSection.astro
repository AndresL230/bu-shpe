---
import SponsorGrid from "@/components/SponsorGrid.astro";
import type { Sponsor } from "@/types";
import supportData from "@/data/support.json";
import sponsorsData from "@/data/sponsors.json";

const sponsors = sponsorsData as Sponsor[];
const corporate = sponsors.filter((s) => !s.name.startsWith("BU "));

const valuePitches = [
  {
    title: "Access to Hispanic STEM talent at a top engineering school",
    description: "Our members study mechanical, electrical, biomedical, and computer engineering at one of the nation's top programs.",
  },
  {
    title: "Visibility at chapter events and the SHPE National Convention",
    description: "Put your brand in front of students at workshops, panels, career fairs, and the largest Hispanic STEM conference in the country.",
  },
  {
    title: "A direct recruiting pipeline to motivated, organized students",
    description: "Skip the resume pile. Meet motivated, organized students who are ready to work.",
  },
];

const partnerBenefits = supportData.partnerBenefits as string[];
---

<!-- Why partner — horizontal pillars (heading on top) -->
<section class="reveal py-20 md:py-28">
  <div class="mx-auto max-w-[1200px] px-6">
    <p class="eyebrow text-accent">WHY PARTNER</p>
    <h2 class="display mt-4 max-w-2xl text-3xl leading-[1.05] text-ink md:text-4xl">
      Meet your next engineers <em class="font-serif italic font-normal lowercase tracking-normal text-accent">first</em>.
    </h2>
    <div class="mt-14 grid gap-10 md:grid-cols-3 md:gap-12">
      {valuePitches.map((pitch, i) => (
        <div class="flex flex-col">
          <span class="font-cond text-4xl leading-none text-accent tabular-nums">0{i + 1}</span>
          <h3 class="display mt-5 text-2xl leading-tight text-ink">{pitch.title}</h3>
          <p class="mt-3 leading-relaxed text-ink-muted">{pitch.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- What partners get -->
<section class="reveal border-t border-outline-soft/50">
  <div class="mx-auto grid max-w-[1200px] grid-cols-1 gap-10 px-6 py-20 md:grid-cols-12 md:gap-16 md:py-28">
    <div class="md:col-span-4">
      <p class="eyebrow text-accent">PARTNERSHIP BENEFITS</p>
      <h2 class="display mt-4 text-3xl leading-[1.05] text-ink md:text-4xl">
        What you get.
      </h2>
    </div>
    <ul class="flex flex-col gap-3 md:col-span-8 md:pt-1">
      {partnerBenefits.map((b) => (
        <li class="flex items-center gap-4 rounded-[var(--radius-card)] border border-rule/30 bg-surface p-5">
          <span class="grid h-9 w-9 shrink-0 place-items-center rounded-full bg-accent/10 text-base font-bold text-accent" aria-hidden="true">✓</span>
          <span class="text-base leading-snug text-ink md:text-lg">{b}</span>
        </li>
      ))}
    </ul>
  </div>
</section>

<!-- Current partners -->
<section class="reveal border-t border-outline-soft/50">
  <div class="mx-auto max-w-[1200px] px-6 py-20 md:py-28">
    <h2 class="display text-3xl leading-[1.05] text-ink md:text-4xl">
      Thank you to our partners.
    </h2>
    <p class="mt-4 max-w-2xl leading-relaxed text-ink-muted">
      With continued support from the BU College of Engineering and the Student Activities Office.
    </p>
    <div class="mt-12">
      <SponsorGrid sponsors={corporate} />
    </div>
  </div>
</section>
